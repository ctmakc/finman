// ==================== SPLIT ====================
async function renderSplitPage() {
  const mainContent = document.getElementById('main-content');
  mainContent.innerHTML = '<div class="loading"><div class="spinner"></div></div>';
  try {
    const groups = await fetchWithAuth('/api/split/groups').then(r => r.json());
    const stats = await fetchWithAuth('/api/split/stats').then(r => r.json());
    mainContent.innerHTML = `<div class="split-page"><div class="page-header"><h1><i class="fas fa-users"></i> Split Expenses</h1><button class="btn btn-primary" onclick="showAddGroupModal()"><i class="fas fa-plus"></i> New group</button></div><div class="stats-grid"><div class="stat-card stat-ok"><div class="stat-icon"><i class="fas fa-arrow-down"></i></div><div class="stat-data"><div class="stat-value">${formatCurrency(stats.totalOwed)}</div><div class="stat-label">They owe me</div></div></div><div class="stat-card stat-over"><div class="stat-icon"><i class="fas fa-arrow-up"></i></div><div class="stat-data"><div class="stat-value">${formatCurrency(stats.totalOwes)}</div><div class="stat-label">I owe</div></div></div><div class="stat-card"><div class="stat-icon"><i class="fas fa-balance-scale"></i></div><div class="stat-data"><div class="stat-value">${formatCurrency(stats.netBalance)}</div><div class="stat-label">Balance</div></div></div></div><div class="card-grid" id="groups-list">${groups.length === 0 ? '<div class="empty-state"><i class="fas fa-users"></i><h3>No groups</h3></div>' : groups.map(g => renderGroupCard(g)).join('')}</div></div>`;
  } catch (e) { mainContent.innerHTML = '<div class="alert alert-error">Failed to load</div>'; }
}
function renderGroupCard(g) { return `<div class="card" onclick="openGroup(${g.id})"><div class="card-header"><h3 class="card-title"><i class="fas fa-users"></i> ${esc(g.name)}</h3></div><p class="text-secondary">${esc(g.description || '')}</p><p>Currency: ${g.currency || 'USD'}</p></div>`; }
async function openGroup(id) { const mainContent = document.getElementById('main-content'); mainContent.innerHTML = '<div class="loading"><div class="spinner"></div></div>'; try { const stats = await fetchWithAuth('/api/split/groups/' + id + '/stats').then(r => r.json()); const balances = await fetchWithAuth('/api/split/groups/' + id + '/balances').then(r => r.json()); const suggested = await fetchWithAuth('/api/split/groups/' + id + '/settlements/suggested').then(r => r.json()); mainContent.innerHTML = `<div class="split-page"><div class="page-header"><button class="btn btn-outline" onclick="renderSplitPage()"><i class="fas fa-arrow-left"></i> Back</button><h1>${esc(stats.group.name)}</h1><button class="btn btn-primary" onclick="showAddExpenseModal(${id})"><i class="fas fa-plus"></i> Add expense</button></div><div class="stats-grid"><div class="stat-card"><div class="stat-data"><div class="stat-value">${stats.memberCount}</div><div class="stat-label">Members</div></div></div><div class="stat-card"><div class="stat-data"><div class="stat-value">${formatCurrency(stats.totalSpent)}</div><div class="stat-label">Total spent</div></div></div><div class="stat-card"><div class="stat-data"><div class="stat-value">${formatCurrency(stats.averagePerPerson)}</div><div class="stat-label">Per person</div></div></div></div><div class="card"><h3>Balances</h3><div class="list-group">${balances.map(b => `<div class="list-item"><span>${esc(b.member.name)}</span><span class="${b.balance >= 0 ? 'text-success' : 'text-error'}">${b.balance >= 0 ? '+' : ''}${formatCurrency(b.balance)}</span></div>`).join('')}</div></div>${suggested.length > 0 ? `<div class="card"><h3>Suggested transfers</h3><div class="list-group">${suggested.map(s => `<div class="list-item"><span>${esc(s.from.name)} → ${esc(s.to.name)}</span><span class="text-warning">${formatCurrency(s.amount)}</span></div>`).join('')}</div></div>` : ''}</div>`; } catch (e) { mainContent.innerHTML = '<div class="alert alert-error">Error</div>'; } }
async function showAddGroupModal() { const m = document.createElement('div'); m.className = 'modal-backdrop'; m.innerHTML = `<div class="modal"><div class="modal-header"><h2>New group</h2><button class="modal-close" onclick="this.closest('.modal-backdrop').remove()">&times;</button></div><div class="modal-body"><form id="group-form"><div class="form-group"><label class="form-label">Name</label><input type="text" name="name" class="form-control" required></div><div class="form-group"><label class="form-label">Description</label><input type="text" name="description" class="form-control"></div><div class="form-group"><label class="form-label">Your name</label><input type="text" name="creator_name" class="form-control" value="Me"></div></form></div><div class="modal-footer"><button class="btn btn-outline" onclick="this.closest('.modal-backdrop').remove()">Cancel</button><button class="btn btn-primary" onclick="saveGroup()">Create</button></div></div>`; document.body.appendChild(m); }
async function saveGroup() { const f = document.getElementById('group-form'); const d = Object.fromEntries(new FormData(f)); try { const r = await fetchWithAuth('/api/split/groups', { method: 'POST', body: JSON.stringify(d) }); if (!r.ok) throw new Error('Error'); document.querySelector('.modal-backdrop').remove(); showNotification('Group created', 'success'); renderSplitPage(); } catch (e) { showNotification(e.message, 'error'); } }
async function showAddExpenseModal(groupId) { const members = await fetchWithAuth('/api/split/groups/' + groupId + '/members').then(r => r.json()); const m = document.createElement('div'); m.className = 'modal-backdrop'; m.innerHTML = `<div class="modal"><div class="modal-header"><h2>New expense</h2><button class="modal-close" onclick="this.closest('.modal-backdrop').remove()">&times;</button></div><div class="modal-body"><form id="expense-form"><input type="hidden" name="group_id" value="${groupId}"><div class="form-group"><label class="form-label">Description</label><input type="text" name="description" class="form-control" required></div><div class="form-row"><div class="form-group"><label class="form-label">Amount</label><input type="number" name="amount" class="form-control" required min="0.01" step="0.01"></div><div class="form-group"><label class="form-label">Who paid</label><select name="paid_by" class="form-control">${members.map(m => `<option value="${m.id}">${m.name}</option>`).join('')}</select></div></div><div class="form-group"><label class="form-label">Date</label><input type="date" name="date" class="form-control" value="${new Date().toISOString().split('T')[0]}"></div></form></div><div class="modal-footer"><button class="btn btn-outline" onclick="this.closest('.modal-backdrop').remove()">Cancel</button><button class="btn btn-primary" onclick="saveExpense()">Add</button></div></div>`; document.body.appendChild(m); }
async function saveExpense() { const f = document.getElementById('expense-form'); const d = Object.fromEntries(new FormData(f)); try { const r = await fetchWithAuth('/api/split/groups/' + d.group_id + '/expenses', { method: 'POST', body: JSON.stringify(d) }); if (!r.ok) throw new Error('Error'); document.querySelector('.modal-backdrop').remove(); showNotification('Expense added', 'success'); openGroup(parseInt(d.group_id)); } catch (e) { showNotification(e.message, 'error'); } }
window.showAddGroupModal = showAddGroupModal; window.saveGroup = saveGroup; window.openGroup = openGroup; window.showAddExpenseModal = showAddExpenseModal; window.saveExpense = saveExpense;
